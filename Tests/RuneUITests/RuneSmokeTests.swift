import XCTest

final class RuneSmokeTests: XCTestCase {
    func testNativeRESTSmokeHarnessIsPending() throws {
        throw XCTSkip("Replace the removed native command-script smoke harness with native fake Kubernetes REST fixtures.")
    }
}
